export interface Meeting {
    id?: number,
    title: string,
    date: string,
    time: string
}

export interface Contact {
    id?: number,
    firstName: string,
    secondName?: string,
    telephone?: string
}
